# Current-interface visual record

Captured September 18, 2026 (Pacific time) from the local current instrument checkout, based on GitHub revision `9eed946`. These are real interface captures, not rendered product mockups.

- `overview.png`: project landing page.
- `lab-setup.png`: experiment configuration in deterministic demonstration mode.
- `lab-evidence.png`: demonstration chart and baseline/perturbation comparison table.
- `timeline.png`: development and recognition section.
- `disclosure.png`: method and data disclosure page.
- `impact.png`: impact interface with local development measurements, not production usage statistics.
- `interface-tour.mp4`: 30-second silent slideshow of these six screenshots, not a continuous interaction recording. Each still is shown for five seconds; different capture sizes are letterboxed.

The local environment has no historical provider credentials. Positive numbers in the demonstration screenshot are illustrative arithmetic, not investment performance or case 001 results. The local development server retained its startup build metadata; these captures demonstrate interface behavior, not an immutable build-provenance record. Consult the live status page and exported artifact for a particular run's identity.

The video can be rebuilt with FFmpeg from `tour.ffconcat`. The earlier August 2026 product film is a separate historical asset, linked and labeled in the repository README.
